# ===================================================================
# 激活配置（可选dev、prod、test)
# ===================================================================
spring:
   profiles:
       active: test